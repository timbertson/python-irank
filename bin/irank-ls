#!/usr/bin/env python

import os, sys
import irank
from irank import config

(_opts, args) = config.IrankOptionParser().parse_args()

for f in args:
	song = irank.Song(f)
	name = os.path.splitext(os.path.basename(f))[0]
	print "%-60s  |%s" % (name[:55], song.values.format_line())
